# Status

This file is a human-readable pointer. The computed status comes from:

```
uv run sb project status ainstein-audit
```

Never write progress percentages here. The dashboard reads repository files only.
