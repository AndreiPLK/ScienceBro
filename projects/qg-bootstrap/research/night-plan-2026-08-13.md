# Night plan 2026-08-13 -> 08-14 (approved mode: science till morning, loop + self-checks)

Founder: "давай науку до утра в луп с повторениями и проверками себя".
Video paused; on resume captions go ENGLISH.

## Slices (in order; each ends with self-check + DATA_LOG + commit)

N1. Fourth trajectory a_{n,n-3}: closed form by the same top-coefficients
    method (three monomials feed P_{n-3}). Goal: show it never binds inside
    the island (like n-2 asymptotically positive), shrinking the conjecture.
    Self-check: brute-force ratio vs formula for n=4..10, random (r,w).

N2. Fixed-spin large-n asymptotics of a_{n,l} for fixed l, w != 0 (extends
    Mansfield-Spradlin Thm 10 to our w-family). If a_{n,l} > 0 for n > N0(r,w)
    with explicit N0 on the island, the completeness conjecture becomes a
    THEOREM modulo a finite checkable window. This is the crown target.
    Self-check: compare asymptotic sign vs exact a_route1 at n=30..60 samples.

N3. D-dependence of the binding low-n curves (a_{2,0}, a_{3,0}, a_{1,l}) via
    exact Gegenbauer projection (even D -> polynomial weight, exact Fractions).
    Output: analytic island description as function of D; spot-verify D=6 map
    on a coarse 0.2 grid, N=10.
    Self-check: D=4 reduction must reproduce the verified curves exactly.

N4. Paper skeleton (English) in article/qg-island-draft.md: title, abstract,
    results list, figure plan (atlas, edge map, killer census), method summary,
    honest-status table per claim. No overclaims: conjecture labeled.

N5. If time remains: q>1 peek (Card B direction) — does the edge law survive
    q-deformation? Symbolic q-residue at small n.

## Loop rules
- ScheduleWakeup ~60 min; each wake: continue current slice, re-check one
  earlier result (rotating: razor zeros -> route agreement -> map spot),
  append DATA_LOG, commit, short report to founder (he is asleep; reports
  accumulate in chat).
- Compute-runner rules for anything > 30 s. No claims promoted at night —
  everything logged as derived/verified-numerically for morning review.

N6 (MORNING DELIVERABLE, founder 23:0x): comic-style video over ALL results —
    "в нашей системе с чёрными линиями", bright, interesting, ENGLISH captions.
    Format: panel-based comic (toon shading, black outlines via toy_style
    S.outline / 2D panels), punchy story: scan -> edge theorem -> 176 zombies
    -> executions -> atlas of 7 islands -> rivals agree (MS) -> "boundary is
    now a formula". Build ~05:30-08:00 after science slices; music per canon.
