# HANDOFF — живое состояние лаборатории (читай ЭТО первым)

**Обновлено: 2026-08-17 11:01** (часы машины основателя).
**Последний коммит на момент записи:** 783ef47 2026-08-17 11:01.
**Репозиторий:** github.com/AndreiPLK/ScienceBro — ПУБЛИЧНЫЙ, работаем в ветке main, отдельные ветки не нужны.

**Как читать этот файл:** он описывает состояние на указанное время. Более
свежее всегда лежит в article/DATA_LOG.md (журнал находок по времени) и в
PASHI_STATUS.txt (что считается прямо сейчас). Если в DATA_LOG есть записи
позже указанного времени — верить журналу, а этот файл считать отставшим.

**Замечание о датах:** в журнале за 17.08 есть пометки «ночь 17->18» —
это ошибка моей оценки времени; фактические часы машины показывали 17.08.
Содержание записей верное, только подписи времени были не из системных часов.

## Где мы в науке

**Программа:** квантово-гравитационный S-матричный бутстрап (NORTH_STAR.md).
Вопрос: обязаны ли согласованные гравитационные амплитуды быть струнными.

**Доказано и опубликовано (4 работы, DOI):** остров с краями, берег
замкнутой струны, мастер-формула, теорема лезвий (нож 3).

**Доказано машинно, ждёт публикации:** теоремы ножей j = 4..12 — каждая
область каждого ножа имеет проходящий артефакт, детерминированный гейт
(`tests/unit/test_theorem_gate.py`) зелёный. Ножи 13-20 в очереди счёта.

**Главная теорема (все ножи сразу):** НЕ доказана. Локализована:
- есть замкнутая гипергеометрическая форма всех скобок (4320 точных проверок,
  `results/chr_closed_form.json`);
- знакопеременная спектральная сумма — точный квадрат (проблема знака в весах);
- задача сведена к одному взвешенному неравенству;
- измерен «запас прочности»: он положителен всегда, но экспоненциально мал,
  отношение margin(j)/margin(j+2) → ~11 (`results/keystone_asymptotics.json`);
- перевал стоит на t*/j ≈ 0.42 — значит работает метод перевала;
- ПОЛУЧЕНО КОНТУРНОЕ ПРЕДСТАВЛЕНИЕ (проверено до 1e-13): P_j/c0 =
  (1/2pi)∮ G(x)^2 Psi(1/x) dtheta — тот самый инструмент, которым доказывали
  положительность Венециано (AEHM/Мансфилд). Знаки в мастер-формуле
  СОКРАЩАЮТСЯ: положительные веса против коэффициентов точного квадрата.

## Свежее (ночь 17->18.08, каньон) — состояние на 02:40 18.08
- Контурное представление получено и проверено (1e-13).
- Перевал = наибольший двойной корень G^2 (r* = 1/a_max^2), гашение при theta=pi.
- Механизм малости: подавление типа Абеля-Плана, показатель pi*alpha, alpha≈0.38
  (совпало с измеренным перевалом 0.40±0.02).
- D=6 — точка Заальшютца для нашего ядра (объясняет литературный порог D<=6).
- Найдены комплексные седла контура (дуга около двойного корня).
- ОТКРЫТЫЙ ПУНКТ: топология наискорейшего спуска — какие седла активны.
  Наивная сумма всех седел даёт 10^254 вместо 10^-4 (явление Стокса).
- Проверено и отброшено: рекуррентность 1-го порядка по j с полиномиальными
  коэффициентами (численный поиск дал ложное ядро, точная проверка убила),
  тест Госпера (в работе). Конвейер ножей ОСТАНОВЛЕН осознанно: 12 теорем
  (ножи 2-13) достаточно как база индукции, дальше перебор бесполезен.
- Отброшено 18 гипотез (см. DATA_LOG) — все с сигнатурами.

## Что делать дальше (архитектура доказательства, по шагам)

1. Записать знакопеременную сумму как контурный интеграл (Меллин-Барнс).
2. Найти комплексный перевал при t = α j, α ≈ 0.42, извлечь главный член
   с ЯВНОЙ положительной константой.
3. Оценить остаток равномерно по λ в каньоне (λ ≈ 7..19, зависит от j).
4. Малые j — машиной (уже сделано для j ≤ 12).
Это скелет Мансфилда (arXiv:2502.20372) с нашими измеренными константами
для сверки каждого шага.

## Инструменты (главное)

- Движок: `projects/qg-bootstrap/lab/prover2_core.py` (точная арифметика на
  flint) + `knife_proof2.py` (мелководье), `knife_tail2.py` (хвосты+глубина),
  `knife_belowdiag2.py` (полоса), `knife_farbelow3.py` (дно, интервальный
  сертификат — работает и за j=9, где старый метод ломался).
- Независимый судья: `lab/z3_judge.py` (чужой движок подтверждает ячейки).
- Провенанс: `lab/provenance.py` — каждый артефакт помечен HEAD + флагом
  «грязного кода»; гейт отвергает артефакты с грязного дерева.
- Скилл: `~/.claude/skills/prover-v2/SKILL.md` — правила скорости и контракт.

## Правила, действующие всегда

- Один тяжёлый процесс за раз; метрикам CPU/RAM не верить (в песочнице лгут),
  здоровье процесса — только по росту лога.
- Любой результат = артефакт + запись в `article/DATA_LOG.md` + коммит.
- Ничего не публиковать наружу без клика основателя (Zenodo, письма).
- Провалы сохраняются с сигнатурами (их уже 14+ — карта тупиков).
- Отчёты основателю: коротко, простым языком, с картинкой на каждой вехе.

## Что показать основателю прямо сейчас

- Живой статус: https://andreiplk.github.io/live.html (и локально
  `live_status.html`).
- Сайт и дашборд обновлены (11 теорем, прогресс ~85%).
- Свежие картинки: `article/visuals/critical-canyon.png`,
  `last-obstacle.png`, `staircase-morning.png`, `perfect-square.png`.
